parameters:
    # Level 2 is the Drupal contrib baseline. Higher levels are achievable
    # but need a cleanup pass on the procedural .module hooks first.
    level: 2

    paths:
        - web/modules/custom
        - web/themes/custom

    excludePaths:
        analyseAndScan:
            - web/themes/custom/byte/node_modules/*
            - web/themes/custom/byte/dist/*
            # Tests use Drupal core base classes that pull a lot of legacy
            # surface; keep them out of analysis until the test suite is
            # established and we can lift level/strictness intentionally.
            - web/modules/custom/*/tests/*

    ignoreErrors:
        # The DI conversion of Block plugins is tracked as a MAJOR follow-up
        # from the audit. Silence here until that PR lands so CI stays green.
        - identifier: globalDrupalDependencyInjection.useDependencyInjection

    treatPhpDocTypesAsCertain: false
    reportUnmatchedIgnoredErrors: false
