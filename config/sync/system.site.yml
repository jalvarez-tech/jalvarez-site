_core:
  default_config_hash: HlN7eAN2N4JIHsYv56V4E7sqC9bS609KwvGFjyD_mgk
langcode: es
uuid: b415abbf-906c-4bbe-9834-1f46c9fa71c9
# NOTE: name, mail and page.front are repaired post-cim by
# scripts/fix-prod-system-site.php (wired into .github/workflows/deploy.yml).
# Values below are the production canonical ones, kept here so a `drush cex`
# from local doesn't silently regress them in the repo. If you reset your
# local DB and re-export config, please restore these three keys to match
# production before pushing.
name: jalvarez.tech
mail: stevanswd@gmail.com
slogan: ''
page:
  403: ''
  404: ''
  # canvas_page IDs differ between envs; fix-prod-system-site.php resolves
  # the home dynamically via path_alias.manager('/inicio') at deploy time.
  front: /page/1
admin_compact_mode: false
weight_select_max: 100
default_langcode: es
mail_notification: null
